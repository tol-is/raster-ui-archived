// const { scaleToRem } = require('./utils/scale-to-rem');

const tailwindPlugin = require('./tailwind-plugin');

const compositor = styledTheme => tailwindConfig => {
  const styledConfig = {};

  return styledConfig;
};

module.exports = compositor;
